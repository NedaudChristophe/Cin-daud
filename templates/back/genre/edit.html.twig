{% extends 'base.html.twig' %}

{% block title %}Edit Genre{% endblock %}

{% block body %}

<div class="container pt-5">

    <div class="row mb-2">
        <div class="col">

            
            <h1 class="text-center my-4">Editer</h1>
            <a class="btn btn btn-primary my-4" href="{{ path('app_back_genre_index') }}">Retour à la liste</a>
            {{ include('back/genre/_form.html.twig', {'button_label': 'Update'}) }}

            {{ include('back/genre/_delete_form.html.twig') }}
            
        </div>
    </div>
</div>



{% endblock %}
