{% extends 'base.html.twig' %}

{% block title %}Genre{% endblock %}

{% block body %}

<div class="container pt-5">

    <div class="row mb-2">
        <div class="col">

    <h1>Genre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>GenreId</th>
                <td>{{ genre.genreId }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ genre.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-sm btn-primary my-4 " href="{{ path('app_back_genre_index') }}">Retour à la liste</a>

    <a class="btn btn-sm btn-warning ms-2" href="{{ path('app_back_genre_edit', {'genreId': genre.genreId}) }}">Editer</a>

    {{ include('back/genre/_delete_form.html.twig') }}
{% endblock %}
